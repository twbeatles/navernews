📰 뉴스 스크래퍼 Pro 업데이트 히스토리

이 문서는 News Scraper Pro 애플리케이션의 개발 및 유지보수 내역을 기록합니다.

🚀 최신 버전 (Current)

v24.6 (2025-11-20)

토스트 메시지 시스템 완성 및 알림 최적화

New Feature: 커스텀 토스트 메시지(Toast Message) 위젯 도입 (반투명, 페이드 인/아웃 애니메이션).

Improvement: 다수 탭 새로고침 시 알림 메시지가 중복으로 뜨는 문제 해결 (Batch Processing 로직 적용).

개별 탭 갱신 시: 즉시 알림.

전체/자동 갱신 시: 모든 작업 완료 후 요약된 메시지 1회 표시.

📜 과거 업데이트 내역

v24.x: UI/UX 디테일 및 버그 수정

v24.1: 설정창 재진입 시 저장된 API 키와 설정값이 비어 보이는 문제 수정 (메모리 데이터 동기화).

v24.0: 날짜 표시 형식을 기계적인 포맷(YYYY-MM-DD)에서 한국어 친화적 포맷(YYYY년 MM월 DD일 HH:MM)으로 변경.

v23.x: 가독성 및 레이아웃 개선

v23.1:

전체적인 글꼴 크기 상향 조정 (제목 13pt, 본문 11pt).

뉴스 카드 내 버튼 그룹(공유, 외부, 메모 등)이 줄바꿈 되지 않고 항상 최우측에 정렬되도록 CSS(white-space: nowrap) 수정.

v23.0: 뉴스 항목 간의 간격(Margin)과 내부 여백(Padding)을 대폭 늘려 가독성 확보.

v22.x: 브라우저 엔진 안정화 & 링크 로직 확립

v22.2: 언론사 표시 로직 개선. "네이버뉴스"라는 단순 표기 대신 chosun.com 등 원본 도메인을 표시하여 출처 식별력 강화.

v22.0 ~ v22.1:

Critical Fix: '공유' 등 기능 버튼 클릭 시 화면이 하얗게 사라지는 문제 완벽 해결.

QTextBrowser의 setOpenLinks(False) 속성을 활용하여 내부 네비게이션을 원천 차단하고, 클릭 시그널만 처리하는 구조로 변경.

링크 클릭 시 **네이버 뉴스(news.naver.com)**가 존재할 경우 원본 링크보다 우선 연결되도록 편의성 증대.

v21.x: 대규모 리팩토링 (구조 혁신)

v21.6: 내부 명령 URL 스키마를 app://action/hash 형태의 표준 URI 구조로 변경하여 파싱 안정성 확보.

v21.3 ~ v21.5: UI 디자인을 사용자가 가장 선호했던 v18.2(최초 버전) 스타일로 100% 복원.

v21.0 (Major Refactor):

구조 변경: 메인 윈도우가 모든 것을 관리하던 방식에서, 각 탭을 독립적인 NewsTab 객체로 분리.

데이터 보호: URL을 통해 데이터를 전달하던 방식에서, 고유한 Hash ID만 전달하고 실제 데이터는 메모리에서 조회하는 방식으로 변경하여 특수문자 오류 및 데이터 소실 원천 차단.

v20.x: 기능별 버그 수정

v20.1: f-string 내 따옴표 불일치로 인한 SyntaxError 수정.

v20.0: '공유' 기능 실행 시 불필요하게 화면 전체를 다시 그리는 로직을 제거하여 깜빡임 현상 해결.

v19.x: 성능 개선 및 기능 추가

v19.6: Base64 인코딩을 도입하여 URL 내 특수문자 처리 문제 해결 시도.

v19.5: 데이터베이스 동시성 문제 해결을 위해 Mutex 락(Lock) 도입.

v19.4: 설정창 콤보박스 초기화 오류 수정 (자동 새로고침이 6시간으로 고정되던 버그 해결).

v19.3: Critical Fix: 스레드 시작 시그널(thread.started) 연결 누락으로 인한 '무한 로딩' 버그 수정.

v19.2: API 에러 핸들링 강화 (HTTP 401, 429 등 구체적 원인 리포팅).

v19.0: CSV 내보내기 기능 추가, DB 연결 방식을 스레드별 독립 연결(threading.local)로 개선하여 안정성 확보.

v18.2 (Base Version)

초기 UI 및 기본 기능 구현 (탭 브라우징, 검색, 북마크).